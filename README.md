# DocKitBot

Telegram-бот для автоматической обработки комплекта юридических документов с поддержкой ZIP архивов и автоматическим исправлением кодировки.

## Возможности

- 📁 Прием документов (PDF, JPG, PNG) и архивов ZIP
- 🔄 Автоматическое выравнивание ориентации документов с помощью OCR
- 📄 Конвертация изображений в PDF
- 📋 Объединение многостраничных документов
- 📦 Создание итогового архива с описью документов
- ⚠️ Детальный отчет об ошибках обработки
- 🔧 **Автоматическое исправление кодировки** имен файлов из ZIP архивов
- 📦 **Автоматическая распаковка ZIP архивов** с поддержкой вложенных папок
- 🎯 **Улучшенное определение ориентации** документов
- 📝 **Систематическое исправление кракозябр** в именах файлов

## Новые возможности

### Автоматическое исправление кодировки
Бот автоматически исправляет проблемы с кодировкой имен файлов:
- Систематическое декодирование из различных кодировок (CP866, Windows-1251, CP1252, ISO-8859-1)
- Автоматическое определение кракозябр
- Словарь замен для часто встречающихся проблем
- Поддержка русских символов в именах файлов

### Улучшенная обработка ZIP архивов
- Автоматическая распаковка архивов
- Исправление кодировки имен файлов и папок
- Поддержка вложенных папок
- Обработка ошибок переименования

### Улучшенное определение ориентации
- Более точное определение ориентации с помощью OCR
- Поддержка русского и английского языков
- Улучшенная обработка EXIF данных
- Автоматическое исправление перевернутых документов

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd DocKitBot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка Tesseract OCR

#### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
```

#### macOS:
```bash
brew install tesseract tesseract-lang
```

#### Windows:
Скачайте и установите с [официального сайта](https://github.com/UB-Mannheim/tesseract/wiki)

### 5. Настройка переменных окружения

Скопируйте `env_example.txt` в `.env` и заполните:

```bash
cp env_example.txt .env
```

Отредактируйте `.env`:
```
TELEGRAM_TOKEN=your_telegram_bot_token_here
```

### 6. Получение Telegram Bot Token

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

## Запуск

```bash
python main.py
```

## Использование

### Основные команды

- `/start` - Начало работы с ботом
- `/help` - Справка по использованию

### Отправка документов

1. **Отдельные файлы**: Отправьте изображения или PDF документы
2. **Архив**: Отправьте ZIP архив с документами (автоматически распакуется)
3. **Смешанный подход**: Отправляйте файлы и архивы в любом порядке

### Автоматическое исправление кодировки

Бот автоматически исправляет проблемы с кодировкой в именах файлов:

**Примеры исправлений:**
- `╨₧╤é╤ç╨╡╤é об отслеживании` → `Отчет об отслеживании`
- `╨¥╨╛╨▓╨░╤Å ╨┐╨░╨┐╨║╨░` → `Новая папка`
- `╨ö╨╛╨│╨╛╨▓╨╛╤Ç` → `Договор`

### Правила именования файлов

#### Примеры корректных имен:
- `Заявка №11 от 11.11.2024 г..jpg`
- `Транспортная накладная №1 от 01.01.2025 г. стр. 1.png`
- `Почтовая квитанция трек №423423422.jpeg`
- `Договор оказания услуг №123 от 15.03.2025 г.pdf`

#### Многостраничные документы:
Используйте маркеры страниц:
- `стр. 1`, `стр. 2` (русский)
- `с. 1`, `с. 2` (сокращенно)
- `page 1`, `page 2` (английский)

### Ограничения

- Максимальный размер файла: 50MB
- Общий размер архива: 100MB
- Поддерживаемые форматы: JPG, JPEG, PNG, PDF, ZIP
- Время обработки: до 30 секунд на файл
- **Максимальное количество файлов в сессии: 50**

## Структура проекта

```
DocKitBot/
├── main.py                 # Главный файл приложения
├── config.py              # Конфигурация
├── bot_handler.py         # Обработчик Telegram бота
├── document_processor.py  # Основной процессор документов
├── file_handler.py        # Обработчик файлов
├── image_processor.py     # Обработчик изображений
├── pdf_converter.py       # Конвертер PDF
├── requirements.txt       # Зависимости
├── env_example.txt        # Пример переменных окружения
├── README.md             # Документация
├── temp/                 # Временные файлы
├── output/               # Результаты обработки
└── logs/                 # Логи
```

## Технологии

- **Python 3.9+**
- **python-telegram-bot** - Telegram Bot API
- **Pillow** - Обработка изображений
- **pytesseract** - OCR для определения ориентации
- **PyPDF2** - Работа с PDF файлами
- **loguru** - Логирование
- **zipfile** - Обработка ZIP архивов
- **chardet** - Определение кодировки (опционально)

## Архитектура

### Основные компоненты

- **BotHandler** - Обработчик Telegram бота с улучшенной системой уведомлений
- **FileHandler** - Обработчик файлов с поддержкой ZIP и исправлением кодировки
- **DocumentProcessor** - Основной процессор документов
- **ImageProcessor** - Обработчик изображений с улучшенным OCR
- **PDFConverter** - Конвертер изображений в PDF

### Система исправления кодировки

1. **Автоматическое определение кракозябр** - поиск типичных символов
2. **Систематическое декодирование** - попытка различных кодировок
3. **Словарь замен** - исправление конкретных проблем
4. **Валидация результата** - проверка на наличие кириллицы

## Логирование

Логи сохраняются в папке `logs/` с ротацией:
- Ротация: ежедневно
- Хранение: 7 дней
- Уровень: INFO

## Безопасность

- Валидация типов файлов
- Ограничение размера файлов
- Автоматическая очистка временных файлов
- Логирование всех операций

## Поддержка

При возникновении проблем:

1. **Проверьте логи** в папке `logs/` - все операции подробно логируются
2. **Убедитесь, что Tesseract установлен** и доступен в системе
3. **Проверьте корректность токена бота** в файле `.env`
4. **Убедитесь, что все зависимости установлены** из `requirements.txt`
5. **Проверьте права доступа** к папкам `temp/` и `output/`

### Частые проблемы

**Проблема**: Файлы с кракозябрами в именах
**Решение**: Бот автоматически исправляет кодировку, но если проблема остается, проверьте логи

**Проблема**: ZIP архив не распаковывается
**Решение**: Убедитесь, что архив не поврежден и содержит поддерживаемые форматы

**Проблема**: Документы неправильно ориентированы
**Решение**: Бот использует OCR для автоматического исправления ориентации

**Проблема**: Не приходят уведомления о загрузке файлов
**Решение**: Проверьте логи на наличие ошибок отправки сообщений

## Лицензия

MIT License
